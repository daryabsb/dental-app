const state = () => ({
    name: 'Darya Ibrahim'
});

const mutations = {};

const actions = {};

const getters = {
    getName(state) {
        return state.name;
    }
};

export default {
    state,
    mutations,
    actions,
    getters
};